.env
*.session
*.session-journal
__pycache__/
*.pyc
.pytest_cache/
.DS_Store
.claude/
data/

# Your channel list is your own research target list — ship only
# channels.example.csv publicly.
channels.csv

# Personal stopword additions (e.g. for a non-English channel set) —
# see trends.py / the README's "Customizing for your channels" section.
stopwords.local.txt

# Legacy/local test data, not part of the public tool.
channels.json
compactmagazin.json
__all_channels.json
